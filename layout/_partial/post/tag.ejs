<% if (post.tags && post.tags.length){ %>
<div class="label-footer">
  <span class="labels-caption">Labels:</span>
  <%- list_tags(post.tags, {
    show_count: false,
    class: 'article-tag',
    style: '<span>',
    separator: ','
  }) %>
</div>
<% } %>
